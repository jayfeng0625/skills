# Commands

Repo-wide canonical commands. Skills read these verbatim — keep them runnable from a clean shell in the repo root.

## test_command

n/a (planned: `tessl eval` per publishable tile, once tessl ships the eval flow)

## lint_command

tessl tile lint <tile-dir>

(e.g. `tessl tile lint tiles/engineering-skills`)

## typecheck_command

n/a

## build_command

tessl tile pack tiles/engineering-skills
