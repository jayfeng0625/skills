There's a new bug report in the tracker. Issue `01-slow-checkout` has been filed: the checkout page takes 8 seconds to load on mobile while desktop is fine. No additional context has been provided by the reporter.

Triage the issue. Read the issue file at `.scratch/slow-checkout/issues/01-slow-checkout.md`, investigate what's known, assign the appropriate category and state. If there's enough information to act on, move it to ready-for-agent and write an agent brief. If the issue is too underspecified for an agent to work from, move it to needs-info and post a triage note explaining what additional information is needed.

Follow the configured issue tracker (see CLAUDE.md and `docs/agents/issue-tracker.md`) for how to record triage state and comments — do not call GitHub or Notion APIs directly.
