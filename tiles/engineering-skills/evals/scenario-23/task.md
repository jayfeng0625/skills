There's a new bug report in the tracker. Issue #1 (slow-checkout) has been filed: the checkout page takes 8 seconds to load on mobile while desktop is fine. No additional context has been provided by the reporter.

Triage the issue. Read the issue file, investigate what's known, assign the appropriate category and state. If there's enough information to act on, move it to ready-for-agent and write an agent brief. If the issue is too underspecified for an agent to work from, move it to needs-info and post a triage note explaining what additional information is needed.

Use the configured workflow backend (see CLAUDE.md) — do not call GitHub or Notion APIs directly.
