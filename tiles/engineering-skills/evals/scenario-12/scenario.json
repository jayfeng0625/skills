{ "description": "to-prd writes a terse PRD from agreed design notes — compresses prose while reproducing load-bearing spec (table, ordering, signature format, prototype state machine) verbatim and merging near-duplicate user stories", "include": ["./inputs"] }
