{ "description": "to-prd synthesizes a PRD from agreed design notes — reproducing load-bearing spec (event-type table, fixed backoff schedule, HMAC signature format, prototype state machine) faithfully, filling the PRD template sections, keeping User Stories extensive, and publishing it to the local markdown issue tracker with the ready-for-agent label (upstream intent)", "include": ["./inputs"] }
