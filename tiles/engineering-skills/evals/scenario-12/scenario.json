{ "description": "to-prd writes a terse PRD from agreed design notes — compresses prose and Implementation Decisions while reproducing load-bearing spec (table, ordering, signature format, prototype state machine) verbatim and keeping User Stories extensive (upstream intent)", "include": ["./inputs"] }
