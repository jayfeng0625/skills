Run the thermo-nuclear code quality review on `inputs/bad-code.ts` and report every issue you find.

For each issue, name the anti-pattern (using the skill's vocabulary), explain why it matters, and propose the most direct simplification that reduces net code complexity — not just a reorganization. Prioritize structural regressions and missed code-judo moves over cosmetic observations.

The code to review is in `inputs/bad-code.ts`. There is no git repository here — `inputs/bad-code.ts` is the target of the review.
