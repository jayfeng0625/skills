# Billing Service

A Node.js subscription-billing service.

## Agent skills

### Domain docs

Single-context: read `CONTEXT.md` + `docs/adr/` at the repo root directly before exploring. They are filesystem conventions; degrade gracefully if absent.
